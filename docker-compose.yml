version: "3.9"

services:
  ephys-manip-link:
    container_name: ephys-manip-link
    build: .
    image: kjy5/ephys-manip-link:latest
    working_dir: /root/ephys-manip-link
    stdin_open: true
    tty: true
    ports:
      - "8080:8080"
    volumes:
      - type: bind
        source: .
        target: /root/ephys-manip-link
#    command: python ephys_manip_link/server.py -s no-e-stop -p 8080